DATABASE_URL="postgresql://postgres:password@localhost:5432/nobet_cizelgesi"

# --- Authentication (required) ---------------------------------------------
# Generate both values with:
#   pnpm --filter @nobet/web auth:hash "<your-admin-password>"
#
# AUTH_SECRET signs the session cookie and must be at least 32 characters.
# Changing it invalidates every active session.
# If either value is missing the app fails CLOSED: every request is rejected.
AUTH_SECRET=""
ADMIN_PASSWORD_HASH=""

NEXT_PUBLIC_GOOGLE_CLIENT_ID="866965837196-pu23906a91os844d1g1k4gsqcesphcrt.apps.googleusercontent.com"
